---
layout: layouts/base.njk
section: home
---

{{ layoutContent | safe }}

<section class="art-grid">
	{% for post in collections.post %}
		<a class="art-grid-item" href="{{ post.url | url }}" style="background-image: url({{ post.data.image }}); background-position: center; background-sizing: cover;"></a>
		{# <article{% if post.url == url %} data-current="current item"{% endif %} class="featured-post">
			<h3 class="featured-post_title">
				<a href="{{ post.url | url }}">
					{% if post.data.title %}
						{{ post.data.title }}
					{% else %}
						Untitled
					{% endif %}
				</a>
			</h3>
			<p class="featured-post_date">
				<time datetime="{{ post.date | machineDate }}">
					<small>{{ post.date | readableDate }}</small>
				</time>
			</p>
			{% if post.data.summary %}
				<p class="featured-post_summary">
					{{ post.data.summary }}
				</p>
			{% endif %}
			<p class="featured-post_read">
				<a href="{{ post.url | url }}">Continue reading →</a>
			</p>
		</article> #}
	{% endfor %}
</section>
